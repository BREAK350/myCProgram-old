//---------------------------------------------------------------------------
#ifndef playerH
#define playerH
#include <vcl.h>
#include "dictionary.h"
#include "field.h"
#include "stack.h"
//---------------------------------------------------------------------------
class T_Player
{
    protected:
        string Name;
        int NWord,NSymbol,Type;
        T_Dictionary Dictionary;
        T_Stack Stack;// стек, де зберігаються координати слова
        int SearchWord(T_Field &field,const char *word,const int &x,const int &y);
        void SearchWord_NextSymbol(T_Field &field,const char *word,const int &i,
            const int &x,const int &y,int &q);
    public:

};
//---------------------------------------------------------------------------
void DoBack(char *w);
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
#endif
