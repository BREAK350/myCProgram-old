//---------------------------------------------------------------------------
#ifndef Unit2H
#define Unit2H
#include <iostream.h>
//---------------------------------------------------------------------------
class T_Room;
typedef T_Room* pT_Room;
class T_Door
{
    private:
        int Max;
        int a;
        int Number;
        pT_Room First,Second;
    public:
        T_Door();
        void Init(const int &,const int &,pT_Room,pT_Room);
        int CanGo() { return a; }
        int Go() { if(CanGo()) { a--; return 1; } return 0; }
        pT_Room Go(pT_Room);
        void Restore();
};
typedef T_Door* pT_Door;
//---------------------------------------------------------------------------
class T_Room
{
    private:
        int N;
        int Number;
        pT_Door *Door;
    public:
        T_Room() { N=0; Door=NULL; Number=0; }
        ~T_Room() { N=0; delete[] Door; Door=NULL; }
        void AddDoor(pT_Door);
        void Go(int *,const int,const int &,int &);
        int Cango();
};
//---------------------------------------------------------------------------
class T_Puzzle
{
    private:
        int nDoor;
        T_Door *Door;
        int nRoom;
        T_Room *Room;
    public:
        T_Puzzle();
        T_Puzzle(const int &,const int &);
        ~T_Puzzle();
};
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
#endif
 