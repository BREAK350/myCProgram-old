//---------------------------------------------------------------------------
#ifndef Unit2H
#define Unit2H
#include <vcl.h>
//---------------------------------------------------------------------------
struct T_Corpus
{
    float x1,y1,x2,y2; // координати прямокутника (корпуса)
    float xp,yp,rp,dp,angle; // центр башні (вісь обертання), радіус башнібдовжина ствола,кут повороту пушки
    TColor Corpus,Pushka;
    T_Corpus();
    T_Corpus(const float &_x1,const float &_y1,const float &_x2,const float &_y2,
             const float &_xp,const float &_yp,const float &_rp,const float &_dp,
             const float &_angle,const TColor &corpus,const TColor &pushka);
    void Init(const float &_x1,const float &_y1,const float &_x2,const float &_y2,
              const float &_xp,const float &_yp,const float &_rp,const float &_dp,
              const float &_angle,const TColor &corpus,const TColor &pushka);
    void Draw(TCanvas *Canvas,const TPoint &Centre);
};
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
#endif
 