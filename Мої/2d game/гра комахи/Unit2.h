//---------------------------------------------------------------------------
#ifndef Unit2H
#define Unit2H
#include "Draw2D.h"
#define ALPHA(x,y) ((x>=0)?((x==0)?(90):(atan(y/x)*180/M_PI)):(atan(y/x)*180/M_PI+180))
//---------------------------------------------------------------------------
class insect
{
    public:
        detail  oform;
        point2D loc,
                go;
        float   v;
        void    DrawInsect(Graphics::TBitmap*);
        void    Move();
};
//---------------------------------------------------------------------------
struct listitem
{
    insect ins;
    listitem *next,*prev;
};
//---------------------------------------------------------------------------
struct list
{
    listitem *first,*last;
};
//---------------------------------------------------------------------------
class MainInsect
{
    private:
        int     n;
    public:
        insect  myinsect;
        list    ins;
        void    CreateListInsect(int,int,char**);
        void    Move();
        void    MoveAll();
        void    DrawAll(Graphics::TBitmap*);
};
//---------------------------------------------------------------------------
void CreateBMP(int,int);
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
#endif
