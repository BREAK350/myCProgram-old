//---------------------------------------------------------------------------
#ifndef Unit3H
#define Unit3H
#include <stdio.h>
#include <conio.h>
#include <string.h>
#include <alloc.h>
//---------------------------------------------------------------------------
int    ChangeWord(char*&,char*,char*);
int    ChangeAllWord(char*&,char*,char*);
int    SearchWord(char*,char*);
int    NumberOfIdenticalWords(char*,char*);
int    SortChar(char**,int);
int    NumberWord(char*);
int    NumberLine(char*);
//---------------------------------------------------------------------------
#endif
